version: "3.9"

services:
  airflow-init:
    image: "apache/airflow:2.9.3"
    container_name: "airflow-init"
    user: "50000:0"
    group_add:
      - "57439"
    entrypoint: "/bin/bash"
    command: >
      -lc "airflow db migrate &&
           airflow users create
           --username ${AIRFLOW_ADMIN_USER}
           --firstname Admin
           --lastname User
           --role Admin
           --email ${AIRFLOW_ADMIN_EMAIL}
           --password ${AIRFLOW_ADMIN_PASSWORD}"
    environment:
      - "TZ=${TZ}"
      - "AIRFLOW__CORE__EXECUTOR=LocalExecutor"
      - "AIRFLOW__CORE__LOAD_EXAMPLES=false"
      - "AIRFLOW__CORE__DAGS_FOLDER=/opt/airflow/dags"
      - "AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://${AIRFLOW_DB_USER}:${AIRFLOW_DB_PASSWORD}@${AIRFLOW_DB_HOST}:${AIRFLOW_DB_PORT}/${AIRFLOW_DB_NAME}"
      - "AIRFLOW__WEBSERVER__EXPOSE_CONFIG=false"
      - "_PIP_ADDITIONAL_REQUIREMENTS=apache-airflow-providers-trino==5.7.1 trino feedparser wikipedia xlrd==2.0.1"
    volumes:
      - "${DATA_PATH}/airflow/dags:/opt/airflow/dags"
      - "${DATA_PATH}/airflow/logs:/opt/airflow/logs"
      - "${DATA_PATH}/airflow/plugins:/opt/airflow/plugins"
      - "/home/lionswrath/data/lake:/data/lake"
    restart: "no"
    env_file:
      - ../envs/base.env
      - ../envs/airflow.env

  airflow-webserver:
    image: "apache/airflow:2.9.3"
    container_name: "airflow-webserver"
    user: "50000:0"
    group_add:
      - "57439"
    command: "webserver"
    environment:
      - "TZ=${TZ}"
      - "AIRFLOW__CORE__EXECUTOR=LocalExecutor"
      - "AIRFLOW__CORE__LOAD_EXAMPLES=false"
      - "AIRFLOW__CORE__DAGS_FOLDER=/opt/airflow/dags"
      - "AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://${AIRFLOW_DB_USER}:${AIRFLOW_DB_PASSWORD}@${AIRFLOW_DB_HOST}:${AIRFLOW_DB_PORT}/${AIRFLOW_DB_NAME}"
      - "AIRFLOW__WEBSERVER__EXPOSE_CONFIG=false"
      - "AIRFLOW__WEBSERVER__SECRET_KEY=${AIRFLOW_WEBSERVER_SECRET_KEY}"
      - "_PIP_ADDITIONAL_REQUIREMENTS=apache-airflow-providers-trino==5.7.1 trino feedparser wikipedia xlrd==2.0.1"
      - "VIRTUAL_HOST=airflow.orochi.chst.dev"
      - "VIRTUAL_PORT=8080"
    volumes:
      - "${DATA_PATH}/airflow/dags:/opt/airflow/dags"
      - "${DATA_PATH}/airflow/logs:/opt/airflow/logs"
      - "${DATA_PATH}/airflow/plugins:/opt/airflow/plugins"
      - "/home/lionswrath/data/lake:/data/lake"
    restart: "unless-stopped"
    env_file:
      - ../envs/base.env
      - ../envs/airflow.env

  airflow-scheduler:
    image: "apache/airflow:2.9.3"
    container_name: "airflow-scheduler"
    user: "50000:0"
    group_add:
      - "57439"
    command: "scheduler"
    environment:
      - "TZ=${TZ}"
      - "AIRFLOW__CORE__EXECUTOR=LocalExecutor"
      - "AIRFLOW__CORE__LOAD_EXAMPLES=false"
      - "AIRFLOW__CORE__DAGS_FOLDER=/opt/airflow/dags"
      - "AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://${AIRFLOW_DB_USER}:${AIRFLOW_DB_PASSWORD}@${AIRFLOW_DB_HOST}:${AIRFLOW_DB_PORT}/${AIRFLOW_DB_NAME}"
      - "_PIP_ADDITIONAL_REQUIREMENTS=apache-airflow-providers-trino==5.7.1 trino feedparser wikipedia xlrd==2.0.1"
    volumes:
      - "${DATA_PATH}/airflow/dags:/opt/airflow/dags"
      - "${DATA_PATH}/airflow/logs:/opt/airflow/logs"
      - "${DATA_PATH}/airflow/plugins:/opt/airflow/plugins"
      - "/home/lionswrath/data/lake:/data/lake"
    restart: "unless-stopped"
    env_file:
      - ../envs/base.env
      - ../envs/airflow.env

  airflow-triggerer:
    image: "apache/airflow:2.9.3"
    container_name: "airflow-triggerer"
    user: "50000:0"
    group_add:
      - "57439"
    command: "triggerer"
    environment:
      - "TZ=${TZ}"
      - "AIRFLOW__CORE__EXECUTOR=LocalExecutor"
      - "AIRFLOW__CORE__LOAD_EXAMPLES=false"
      - "AIRFLOW__CORE__DAGS_FOLDER=/opt/airflow/dags"
      - "AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://${AIRFLOW_DB_USER}:${AIRFLOW_DB_PASSWORD}@${AIRFLOW_DB_HOST}:${AIRFLOW_DB_PORT}/${AIRFLOW_DB_NAME}"
      - "_PIP_ADDITIONAL_REQUIREMENTS=apache-airflow-providers-trino==5.7.1 trino feedparser wikipedia xlrd==2.0.1"
    volumes:
      - "${DATA_PATH}/airflow/dags:/opt/airflow/dags"
      - "${DATA_PATH}/airflow/logs:/opt/airflow/logs"
      - "${DATA_PATH}/airflow/plugins:/opt/airflow/plugins"
      - "/home/lionswrath/data/lake:/data/lake"
    restart: "unless-stopped"
    env_file:
      - ../envs/base.env
      - ../envs/airflow.env

networks:
  default:
    name: lsio
    external: true
